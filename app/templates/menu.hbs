<ul id="sideMenu" class="side-nav fixed teal lighten-3">
	<li>
		<div class="userView teal darken-2">
			<a href="#!email">
				<span class="white-text email">
					<h5>mhernan157@hotmail.com</h5>
				</span>
			</a>
		</div>
	</li>
	{{#each menuItems}}
	<li class="sideMenuItem">
		<a href="#" onclick="changeState('{{state}}')" id="{{state}}Link" class="menuLinks">
			<i class="material-icons black-text">{{icon}}</i><span class="black-text">{{item}}</span>
		</a>
	</li>
	{{/each}}
</ul>
